site_name: Python lessons

theme:
  name: "material"

plugins:
- search
- mkdocstrings:
    default_handler: python
    handlers:
      python:
        options:
          show_root_heading: false
          show_source: true
          show_docstring_parameters: true

nav:
  - "Check is instance": check_is_instance.md